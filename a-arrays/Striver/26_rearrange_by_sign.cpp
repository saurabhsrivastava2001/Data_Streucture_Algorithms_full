#include <iostream>
#include <vector>
using namespace std;

void arrange(vector<int> &arr){
    int i=0;
    int j=1;
    for (int x=0;x<arr.size();x++){
        if (arr[x]>0){
            arr
        }
    }
}


int main() {
    int n;
    cin >> n;
    vector<int> arr(n);
    for (int i = 0; i < n; ++i) {
        cin >> arr[i];
    }
    
    // You can process the array here

    return 0;
}